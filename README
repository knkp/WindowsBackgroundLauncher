Just a little tool to run in the background of a windows machine that launches
programs via user-defined hot-keys

The config file has been implemented, although it only currently
supports launching a single program and it also can't deal with
a system path with space's, but it does work otherwise.
